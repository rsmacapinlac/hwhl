{% from "sensu/service_map.jinja" import services with context -%}
{
  "checks": {
    "salt_master_check": {
      "handlers": ["default","slack"],
      "command": "/etc/sensu/plugins/check-procs.rb -p salt-master -C 1 ",
      "interval": 60,
      "subscribers": [ "saltmaster" ]
    }
  }
}
