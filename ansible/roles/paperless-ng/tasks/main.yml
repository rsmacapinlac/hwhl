---
# Environment file validation
- name: Check if environment file exists
  ansible.builtin.stat:
    path: "{{ playbook_dir }}/../homelab_config/ansible/files/config/paperless-ng/environment"
  register: env_file
  delegate_to: localhost
  become: false

- name: Fail if environment file doesn't exist
  fail:
    msg: |
      Environment file not found at {{ playbook_dir }}/../homelab_config/ansible/files/config/paperless-ng/environment
      Please run site-setup.yml first to create the environment file.
  when: not env_file.stat.exists

# Container status checking
- name: Check if paperless-ng container exists
  community.docker.docker_container_info:
    name: paperless-ng
  register: service_container
  ignore_errors: true

# Directory creation with proper permissions (MUST run unconditionally)
- name: Create services folder
  become: true
  ansible.builtin.file:
    path: /data/services/paperless-ng
    state: directory
    owner: ansible
    group: docker
    mode: '0755'

# Environment file deployment (triggers handler when changed)
- name: Copy environment configuration
  copy:
    src: "{{ playbook_dir }}/../homelab_config/ansible/files/config/paperless-ng/environment"
    dest: /data/services/paperless-ng/.env
    owner: ansible
    group: docker
    mode: '0640'
  notify: restart paperless-ng

# Docker Compose deployment (MUST run unconditionally for handlers to work)
- name: Setup docker compose file for paperless-ng
  template:
    src: docker-compose.j2
    dest: /data/services/paperless-ng/compose.yml
    owner: ansible
    group: docker
    mode: '0644'
  notify: restart paperless-ng

# Container deployment using docker_compose_v2 (ONLY this task is conditional)
- name: Run paperless-ng container
  community.docker.docker_compose_v2:
    project_src: /data/services/paperless-ng
    files:
      - compose.yml
    state: present
    remove_orphans: false
  when: not service_container.container.State.Running | default(false)
  notify: restart paperless-ng
