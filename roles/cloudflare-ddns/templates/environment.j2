# Cloudflare DDNS Environment Configuration
# Automatically updates DNS records for dynamic IP addresses

# Cloudflare API Configuration
API_KEY={{ cloudflare_api_key }}

# Zone and Subdomain Configuration
ZONE={{ cloudflare_zone }}
SUBDOMAIN={{ cloudflare_subdomain }}
